# Plauvia — https://www.plauvia.com
#
# The app's own screens are not listed here because robots.txt is not a
# security control: everything private is behind authentication and row-level
# security, and the pages themselves carry a noindex directive, which is the
# one a crawler is obliged to honour once it has the page.

User-agent: *
Allow: /

# Nothing useful for a search result, and a crawler following them just burns
# crawl budget on the same application shell.
Disallow: /login
Disallow: /signup
Disallow: /app
Disallow: /*?go=

Sitemap: https://www.plauvia.com/sitemap.xml
